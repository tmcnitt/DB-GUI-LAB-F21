export class Source {
    constructor(name, base_url, owner_name, bias) {
        this.name = name;
        this.base_url = base_url;
        this.owner_name = owner_name;
        this.bias = bias;
    }
}